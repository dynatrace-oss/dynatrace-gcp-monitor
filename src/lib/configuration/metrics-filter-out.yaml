# Metrics containing the following names will be excluded from the fetch

# If you are willing to filter out specific dimension place it under `dimensions` keyword.
# If you are willing to filter out entire metrics no matter what dimnesion it contains just don't place `dimensions` keyword.

#Example Start:
# - metric: serviceruntime.googleapis.com/api/request_count
#   dimensions:
#     - protocol
#     - response_code
#     - response_code_class
#     - grpc_status_code
# - metric: serviceruntime.googleapis.com/quota/exceeded
#   dimensions:
#     - limit_name
#     - quota_metric
#Example end.

filter_out:
- metric: google_metric_1
- metric: google_metric_2
  dimensions:
    - dimension2_1
    - dimension2_2
    - dimension2_3
- metric: google_metric_3
  dimensions:
    - dimension3_1